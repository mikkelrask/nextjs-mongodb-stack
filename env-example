# NextJS - if you have services running on port 3000 change this
NEXTJS_PORT=3000

# Database - set your desired database username, password and database name. 
# Port is defaulting to 27017 and can be changed if needed
MONGO_USER=username
MONGO_PASS=password
MONGO_DB_NAME=datase
MONGO_PORT=27017

# Git - Provide the github URL for your repo
# GIT_USER and GIT_PASS are needed if your repo is set as private
REPO_URL=https://github.com/username/repo.git
GIT_USER=
GIT_PASS=

